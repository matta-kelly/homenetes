# ==============================================================================
# Vaultwarden Secrets
# ==============================================================================
# IMPORTANT: Encrypt this file with SOPS before committing!
#   sops -e -i vaultwarden/secrets.yaml
#
# Generate admin token with: openssl rand -base64 48
# ==============================================================================

apiVersion: v1
kind: Secret
metadata:
  name: vaultwarden-secrets
  labels:
    app.kubernetes.io/name: vaultwarden
type: Opaque
stringData:
  # Admin panel access token - generate with: openssl rand -base64 48
  admin-token: "REPLACE_ME_THEN_ENCRYPT"
