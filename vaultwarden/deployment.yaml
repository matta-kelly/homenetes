# ==============================================================================
# Vaultwarden Deployment
# ==============================================================================
# Single-replica deployment with SQLite storage
# Uses Recreate strategy - SQLite doesn't support concurrent writes
# ==============================================================================

apiVersion: apps/v1
kind: Deployment
metadata:
  name: vaultwarden
  labels:
    app.kubernetes.io/name: vaultwarden
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: vaultwarden
  template:
    metadata:
      labels:
        app.kubernetes.io/name: vaultwarden
    spec:
      # Place on node with bulk storage
      nodeSelector:
        storage.h-kube.io/bulk: "true"

      containers:
        - name: vaultwarden
          image: vaultwarden/server:1.32.7
          ports:
            - name: http
              containerPort: 80
              protocol: TCP

          env:
            # Domain must match external URL exactly
            - name: DOMAIN
              value: "https://vaultwarden.${HOMENETES_DOMAIN}"

            # Registration controls
            - name: SIGNUPS_ALLOWED
              value: "false"
            - name: INVITATIONS_ALLOWED
              value: "true"

            # Admin panel - token from secret
            - name: ADMIN_TOKEN
              valueFrom:
                secretKeyRef:
                  name: vaultwarden-secrets
                  key: admin-token

            # Performance and logging
            - name: ROCKET_WORKERS
              value: "4"
            - name: LOG_LEVEL
              value: "info"

          volumeMounts:
            - name: data
              mountPath: /data

          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi

          livenessProbe:
            httpGet:
              path: /alive
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30

          readinessProbe:
            httpGet:
              path: /alive
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10

          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            runAsNonRoot: true
            runAsUser: 1000
            runAsGroup: 1000

      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: vaultwarden-data
